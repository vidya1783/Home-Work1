const DEFAULT_QUESTION_COUNT = 10;

let questionCount = DEFAULT_QUESTION_COUNT;

let problem = {
    number: 0,
    left: 0,
    right: 0,
    correctAnswer: 0
}
document.addEventListener('DOMContentLoaded', contentLoaded);

function contentLoaded(){
// SETUP THE TEST

// ASSIGN VALUES TO TEST SET ( array of type problem??)

// ATTACHED EVENTS

}